// Canned lines for the [PLAY] nudge picker -- picking one sends it to the
// other device with no typing required. Kept in sentence case (unlike the
// app's usual ALL-CAPS bracket buttons) since these are meant to read as a
// genuine, warm note rather than a system command. See CLAUDE.md's "Shared
// pet" section for why PLAY became this instead of a mini-game.
export const NUDGE_OPTIONS = ['Thinking of you', 'Miss you', "Hope today's going okay"]
